def fetch(url):
    # TODO(PROJ-41): retry on a 429 before giving up
    return url


def parse(body):
    # TODO(PROJ-42): drop this once the new parser lands
    return body


def report():
    # FIXME(PROJ-99): the tracker has never heard of this one
    return None
