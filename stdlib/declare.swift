// gate stdlib declare v1: the heads a declared side is printed under
// role: gate's own
// speaks-for: the-tool
//
// Two texts this tool WRITES into somebody's repository when they declare
// a side of a seam: one for a contract, one for a carrier. They live here
// for the reason the layout's head does: a text kept in two places is two
// texts the day somebody improves one of them, and both carriers write
// these.
//
// Each section begins at its own mark. The card above is the shelf's, not
// the world's.
//
// ── what a contract side is printed under begins here ──
// printed by gate declare: one side of a seam, stated rather than found.
// Whoever runs this owns what it says: it is a view of a document they publish,
// emitted by their own build and signed by merging it. Nothing here was read out
// of anybody's source, because judgement has no jurisdiction over a world that
// has not entered. This file is the entering.
//
//
// ── what a carrier side is printed under begins here ──
// printed by gate declare carrier: what a library says it carries, emitted from
// what that library already keeps: its own generated schema, its own types. The
// emitter lives on its side and belongs to it. This only renders what it said
// into the grammar both sides are judged in. Where its own name for a field
// differs from the contract's, the correspondence is stated too, and only
// where it does not follow, since a declaration that restates what already
// matches is a second copy, and a second copy drifts.
//
